% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discards_rdbfis.R
\name{discards_rdbfis}
\alias{discards_rdbfis}
\title{Discard LFD raising}
\usage{
discards_rdbfis(
  sampling_df,
  landings_df,
  SP = NULL,
  GSA = NULL,
  YEAR = NULL,
  method = NULL,
  sample_types = c("S", "D", "M"),
  use_mat = FALSE,
  out_dir = "output",
  discard_ratio_out_dir = NULL,
  discard_ratio_df = NULL
)
}
\arguments{
\item{sampling_df}{data.frame. Clean RCG sampling template.}

\item{landings_df}{data.frame. Clean RCG landings template.}

\item{SP}{optional character vector of species FAO 3-alpha codes.
Default is \code{NULL}, which processes all species available in the data.}

\item{GSA}{optional character vector of GSA codes. Default is \code{NULL}, which
processes all areas available in the data.}

\item{YEAR}{optional numeric vector of years. Default is \code{NULL}, which
processes all years available in the data.}

\item{method}{optional character. Raising method to use. Default is \code{NULL},
which applies the Member State-specific method. Accepted alternatives
include \code{"trip_h"}, \code{"trip_cc"}, and \code{"vigneau_mahevas"}.}

\item{sample_types}{optional character vector of sampling types to use in
the raising. Default is \code{c("S", "D", "M")}.}

\item{use_mat}{logical. If \code{TRUE}, samples collected for maturity are kept
in the raising. Default is \code{FALSE}.}

\item{out_dir}{character. Output folder where reports, plots, and raised
discard LFDs are written. Default is \code{"output"}.}

\item{discard_ratio_out_dir}{character or \code{NULL}. Output folder for the
discard ratio table created internally. When \code{NULL}, a separate
\code{discard_ratio} folder is created next to \code{out_dir} when possible.}

\item{discard_ratio_df}{data.frame or \code{NULL}. Optional discard-ratio table
previously returned by \code{\link[=discard_ratio_rdbfis]{discard_ratio_rdbfis()}}. When supplied, the table
is reused and the function does not write a second discard-ratio file.}
}
\value{
A data.frame with raised discard LFDs in long format. The function
also writes MED&BS discard-template outputs and subsample-weight
diagnostics to \code{out_dir}.
}
\description{
Prepare cleaned RCG sampling and landings templates and raise discard
length-frequency distributions (LFDs).
}
\details{
This function is the main discard LFD raising step in the stat4RDBFIS
workflow. It estimates discard ratios from cleaned sampling data, combines
them with landings totals, applies the selected raising method, and writes
raised discard LFD outputs.
}
\examples{
\dontrun{
discards_rdbfis(
  sampling_df,
  landings_df,
  SP = c("DPS", "HKE"),
  GSA = "GSA22",
  YEAR = c(2023, 2024),
  method = "trip_cc"
)
}
}
\author{
stat4RDBFIS Core Team
}
