% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/landings_rdbfis.R
\name{landings_rdbfis}
\alias{landings_rdbfis}
\title{Landings LFD raising}
\usage{
landings_rdbfis(
  sampling_df,
  landings_df,
  SP = NULL,
  GSA = NULL,
  YEAR = NULL,
  method = NULL,
  sample_types = c("S", "D", "M"),
  use_mat = FALSE,
  out_dir = "output"
)
}
\arguments{
\item{sampling_df}{data.frame. Clean RCG sampling template.}

\item{landings_df}{data.frame. Clean RCG landings template.}

\item{SP}{optional character vector of species FAO 3-alpha codes.
Default is \code{NULL}, which processes all species available in the data.}

\item{GSA}{optional character vector of GSA codes. Default is \code{NULL}, which
processes all areas available in the data.}

\item{YEAR}{optional numeric vector of years. Default is \code{NULL}, which
processes all years available in the data.}

\item{method}{optional character. Raising method to use. Default is \code{NULL},
which applies the Member State-specific method. Accepted alternatives
include \code{"trip_h"}, \code{"trip_cc"}, \code{"vigneau_mahevas"}, \code{"method_grc"},
\code{"method_ita2"}, \code{"method_ita3"}, \code{"method_esp"}, \code{"method_cyp"}, and
\code{"method_fra"}.}

\item{sample_types}{optional character vector of sampling types to use in
the raising. Default is \code{c("S", "D", "M")}.}

\item{use_mat}{logical. If \code{TRUE}, samples collected for maturity are kept
in the raising. Default is \code{FALSE}.}

\item{out_dir}{character. Output folder where reports, plots, and raised
landings LFDs are written. Default is \code{"output"}.}
}
\value{
A data.frame with raised landings LFDs in long format. The function
also writes MED&BS landings-template outputs and subsample-weight
diagnostics to \code{out_dir}.
}
\description{
Prepare cleaned RCG sampling and landings templates and raise landings
length-frequency distributions (LFDs).
}
\details{
This function is the main landings raising step in the stat4RDBFIS workflow.
It builds the stock and stratum inputs, checks observed against estimated
subsample weights, applies the selected raising method, and writes the
raised landings LFD outputs.
}
\examples{
\dontrun{
landings_rdbfis(
  sampling_df,
  landings_df,
  SP = c("DPS", "HKE"),
  GSA = "GSA22",
  YEAR = c(2023, 2024),
  method = "trip_cc"
)
}
}
\author{
stat4RDBFIS Core Team
}
