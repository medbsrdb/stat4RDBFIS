% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alk_rdbfis.R
\name{alk_rdbfis}
\alias{alk_rdbfis}
\title{Age-Length Key estimation}
\usage{
alk_rdbfis(
  sampling_df,
  SP = NULL,
  GSA = NULL,
  YEAR = NULL,
  length_bin = 10,
  out_dir = getwd()
)
}
\arguments{
\item{sampling_df}{A data.frame containing the cleaned sampling template.}

\item{SP}{Optional character vector of species codes. Default is \code{NULL}.}

\item{GSA}{Optional character vector of GSA codes. Default is \code{NULL}.}

\item{YEAR}{Optional numeric vector of years. Default is \code{NULL}.}

\item{length_bin}{Numeric. Width of the length bin in mm. Default is \code{10}.}

\item{out_dir}{Character. Output directory where csv files and plots are
written.}
}
\value{
A list with components:
\itemize{
\item \code{alk_long}: ALK in long format.
\item \code{alk_wide}: ALK in wide format.
\item \code{naa_long}: Numbers-at-age by length bin.
\item \code{naa}: Numbers-at-age in long format, aggregated by age.
\item \code{naa_wide}: Numbers-at-age in wide format.
\item \code{coverage_summary}: Coverage diagnostics for the ALK application.
}

The function also writes these files to \code{out_dir}:
\itemize{
\item \code{alk_long_*.csv}: ALK in long format.
\item \code{alk_wide_*.csv}: ALK in wide format.
\item \code{naa_long_*.csv}: Estimated numbers-at-age by length bin.
\item \code{naa_wide_*.csv}: Estimated numbers-at-age in wide format.
\item \code{coverage_summary_*.csv}: Coverage diagnostics.
\item \code{alk_heatmap_*.png}: ALK heatmap.
\item \code{alk_lfd_*.png}: Length-frequency plot used for ALK application.
}
}
\description{
Create an age-length key (ALK) from sampling data and apply it to the
observed numbers-at-length in order to estimate numbers-at-age.
}
\details{
The function works on the cleaned sampling template and follows the same
filtering logic used in the other stat4RDBFIS modules. Length classes are
grouped into bins of user-defined width (default 10 mm). The ALK is computed
as P(age | length class) and then applied to the corresponding
\code{number_at_length} values.
}
\author{
stat4RDBFIS Core Team
}
